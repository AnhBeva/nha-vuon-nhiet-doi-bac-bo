# Nguồn ảnh trực quan

Bộ ảnh hiện tại trong `assets/visuals/chapters/` là ảnh generated bằng image generation cho project này. Ảnh được lưu cục bộ trong workspace và không dùng logo Lumi giả, watermark hoặc chữ nhúng.
